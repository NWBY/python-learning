# Tuples are immutable, once they're created they cannot be changed at all
# Create tuples with ()
monthYear = (9, 1997)

# Access values in a tuple using square bracket index notation: [0]
print(monthYear[0])


