# Get length
name = 'Sam'
length = len(name)

# Index of - will always return first instance of letter if string contains multiple instances
index = name.index('a')

# Count number of instances of string
sCount = name.count('S')

# Slice of string [start:stop:step]
hw = 'hello world'
sub = hw[1:4]

# Reverse string with negative step in slice
rev = hw[::-1]
